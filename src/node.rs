//! The blockchain node
use crate::Blockchain;

struct Node {
    chain: Blockchain,
}

impl Node {
    pub fn new() -> Self { unimplemented!() }

    /// Mines a new block
    pub fn mine() { unimplemented!() }

    /// Adds a new transaction
    pub fn new_transaction() { unimplemented!() }

    /// Displays the full blockchain
    pub fn display() { unimplemented!() }
}